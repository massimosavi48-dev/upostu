# UPOSTU

UPOSTU is a smart parking platform with:

- A FastAPI backend (`backend/app`)
- A Progressive Web App frontend (`frontend/pwa`) using Leaflet maps
- A placeholder AI engine (`ai-engine`)
- Docker and infra scaffolding (`docker`, `infra`)

